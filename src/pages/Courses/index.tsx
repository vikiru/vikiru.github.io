import { CourseContainer, Layout } from '~components/index';

function CoursesPage() {
    return (
        <Layout>
            <CourseContainer />
        </Layout>
    );
}

export default CoursesPage;
